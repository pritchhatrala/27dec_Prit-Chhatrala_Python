keys = ["name", "age", "city", "Language"]
values = ["Prit", 21, "Kehsod", "Python"]

merged_dict = {}

for i in range(len(keys)):
    merged_dict[keys[i]] = values[i]

print("Merged Dictionary:", merged_dict)
